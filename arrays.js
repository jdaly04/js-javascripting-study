'use strict';

let pizzaToppings = ['tomato sauce', 'cheese', 'pepperoni'];
  console.log(pizzaToppings);

  //values within an array are called "elements". An array can have elements
  // that are strings/numbers/other arrays

  // .length givesyouthelengthofthearrayyoucalliton.
  //.push() addsanewelementtotheendofanarray,andreturnsthatelement.
//.pop() removes the last element in an array, and returns that element.
//.indexOf() searches within your array for the first element that matches its
//parameter,and returns the index of that match; if no match is found, it
//returns - 1.
